# Repo state when the previous session stopped

Date of freeze: **2026-09-15**. Branch: whatever `main` is locally; **nothing from this Tier 1 work is committed**. `git status` at freeze listed the files below. Treat untracked files as part of the intended delivery — they are not leftovers.

The previous agent was mid-implementation of **contextualization UI + ISA mapping**. The user then asked to **stop coding** and write this handoff.

---

## What is already done (do not redo)

### 1. Local MCP server — DONE

| Path | Role |
|---|---|
| `server/mcp/server.ts` | `McpServer` registering tools + `pharmaops://agent/guardrails` resource |
| `server/mcp/index.ts` | stdio entry (`npm run mcp`); logs to **stderr**; loads dotenv |
| `server/mcp/server.test.ts` | Vitest via `InMemoryTransport`; advertises 12 tools; guardrails resource; B-104 list/events/triage |
| `mcp.json.example` | Cursor / Claude Desktop config template |

Tools (keep this list; CI tests equality):

```
ask_documents, assess_data_quality, build_evidence_packet, get_anomaly_windows,
get_batch_summary, get_operator_notes, get_work_orders, list_batches,
query_events, query_time_series_datapoints, query_time_series_stats, triage_batch
```

Mirrors Industrial MCP's three families: graph query, time-series datapoints, documents, plus a composed `triage_batch` that runs the existing orchestrator (guardrails included).

**Known issue (partially patched, not re-verified):** `callTool` returns a union type. `firstText` was changed to accept `unknown` and pull `.content`. `npm run typecheck` was started after that patch and **aborted** — you must re-run it. If it still fails, keep the `unknown` helper; do not start wrapping every `callTool` result in `as`.

### 2. Atlas AI agent-as-code export — DONE

`scripts/export-atlas-agent.ts` (`npm run export:atlas`).

Generates (header: `# GENERATED by scripts/export-atlas-agent.ts — do not hand-edit`):

```
cdf/modules/pharmaops_deviation/agents/
  pharmaops_triage.Agent.yaml
  pharmaops_deviation_triage.Skill.yaml
  pharmaops_deviation_triage.Skill.md
cdf/agents/pharmaops-triage/
  pharmaops-triage.agent.yaml
  eval/eval.yaml
```

Implementation notes you must preserve:

- Import is `import { dump as yamlDump } from "js-yaml"` — **not** `import yaml from "js-yaml"` (ESM named export; the default import throws).
- `yamlDump(..., { lineWidth: -1, noRefs: true, quotingType: '"' })` so multiline strings stay literal blocks.
- Agent instructions are built from `server/agent/guardrails.ts` (`AGENT_IDENTITY`, `UNIVERSAL_RULES`, `HUMAN_REVIEW_DISCLAIMER`).
- Eval `faithfulness.groundTruth` is built by calling `buildEvidencePacket` for each case in `evals/eval_cases.json`.
- Toolkit agent tools are Atlas types: `query`, `queryTimeSeriesDatapoints`, `askDocument`.
- Model field: `azure/gpt-4.1` (Cognite's Atlas model id, not `gpt-4.1-mini`).
- Skill sidecar is Agent Skills `SKILL.md` format with YAML frontmatter `name` + `description`.

**Missing next to the agents (this is remaining Tier 1):** Toolkit `data_modeling/` and `streams/` under the same module. See `TIER1.md` task T1-A.

### 3. Messy IT/OT/ET data + contextualization — DONE

Pipeline:

```
scripts/generate_synthetic_pharma_data.py  →  data/raw/**  (silos)
scripts/contextualize.py                   →  data/generated/** + contextualization_report.json
scripts/copy-data-to-public.mjs            →  public/data/generated/**
```

`package.json` scripts:

```
"generate-data": "python3 scripts/generate_synthetic_pharma_data.py && python3 scripts/contextualize.py && node scripts/copy-data-to-public.mjs"
"contextualize": "python3 scripts/contextualize.py && node scripts/copy-data-to-public.mjs"
"mcp": "tsx server/mcp/index.ts"
"export:atlas": "tsx scripts/export-atlas-agent.ts"
```

#### Intentional mess in `data/raw/` (do not "clean up")

| Silo | Layer | Files | Deliberate dirt |
|---|---|---|---|
| Engineering register | ET | `engineering/asset_register.csv`, `equipment_register.csv`, `instrument_index.csv`, `operating_limits.csv` | Canonical tags (BIO-101, PH-101, …) |
| MES | IT | `mes/batch_records.csv`, `batch_events.csv`, `operator_log.csv` | `DD-MON-YYYY HH:MM:SS`; unit aliases (`BR-A` for BIO-101); MES status/phase vocab; **duplicated** batch-start event `EVT-B104-004`; **incomplete timestamp** on `NOTE-B104-001` (time only `10:20`); blank severity on a PROC event |
| Historian | OT | `historian/tags.csv`, `datapoints.csv`, `alarms.csv`, `batch_event_frames.csv` | UTC epoch ms; tag names like `CHI.BIO101.PH101.PV`; **°F on CIP return temp** (`CHI.CIP201.TT202.RET.PV`); **orphan tag** `CHI.WFI.FT301.PV`; alarm `CLASS` carries original event category |
| CMMS | IT | `cmms/equipment_master.csv`, `work_orders.json` | SAP equipment numbers + FUNC_LOC; mixed `created_on`/`created_at`; **WO-744 two revisions**; **orphan** equipment `10009999` / `CHI-UTIL-WFI-PMP301` and **WO-760** against it |
| QMS | IT | `qms/deviations.json`, `quality_events.json`, `document_register.json` | ISO-8601 with milliseconds; own severity/state vocab |
| Analytics | derived | `analytics/anomaly_windows.json` | Already in unified ids |

Plant clock: `PLANT_UTC_OFFSET_HOURS = -5` (Chicago in June).

#### Report numbers at freeze (assert these after `generate-data`)

From `data/generated/contextualization_report.json` `summary`:

```
sources: 6
sourceRecords: 56          # file/row counts of small tables, NOT historian datapoints
matches: 49
matchesByMethod: alias 2, exact 20, master_data_join 8, normalized 19
lowConfidenceMatches: 0
unresolved: 3
conversions: 6
duplicatesRemoved: 2
repairs: 2
```

Unresolved (must remain excluded from the model — they are the demo of "we don't silently invent links"):

1. Historian tag `CHI.WFI.FT301.PV` — no instrument-index entry; datapoints not loaded.
2. CMMS `CHI-UTIL-WFI-PMP301` / equipment no. `10009999` — no engineering tag.
3. Work order `WO-760` — same orphan equipment; order excluded.

Duplicates:

- MES event `EVT-B104-004` — kept first occurrence, dropped 1.
- CMMS `WO-744` — kept revision 2, dropped 1.

Repairs:

- `EVT-B104-003` severity blank → defaulted `info`.
- `NOTE-B104-001` timestamp time-only → date inferred from B-104 start.

Conversions include: MES timestamps, historian epoch→plant local (alarms + 5426 datapoints), QMS ms precision, CMMS YYYYMMDD, **DEGF → °C** on CIP return temp (271 points).

`sourceRecords: 56` is **not** the datapoint count. The app panel label was changed to **"Source rows (excl. datapoints)"** for this reason. Do not "fix" the Python summary by summing datapoints into `sourceRecords` unless you also update the UI copy — reviewers will think 56 is a bug.

#### Bugs already fixed in the pipeline (do not regress)

1. **Historian alarms on sensors:** `build_signals` must map a historian tag whose instrument (e.g. PH-101) **is itself equipment** to that instrument, not the parent unit BIO-101. Otherwise alarms land on the bioreactor and evals/UI break.
2. **Event category:** historian `alarms.csv` has a `CLASS` column from `MES_EVENT_TYPE[e["category"]]`. `build_events` must use that for `category`, not hard-code `"alarm"`. One process event was being misclassified as alarm.

Re-run `npm run generate-data` after any Python edit, then `npm run eval` (18/18) and `npm run check-data-sync`.

### 4. Contextualization UI — MOSTLY DONE, not browser-verified

| Path | Status |
|---|---|
| `app/src/types/contextualization.ts` | New. Matches report JSON camelCase keys |
| `app/src/components/ContextualizationPanel.tsx` | New. Fetches `/data/generated/contextualization_report.json` |
| `app/src/pages/DataModelPage.tsx` | Renders panel **above** `CdfReadinessPanel` |
| `app/src/types/cdfMapping.ts` | Rewritten for ISA-88/95, Records, Industrial MCP |
| `app/src/components/CdfReadinessPanel.tsx` | New today/future flows + integration steps + ISA/Records links |
| `site/src/sections/CdfReadiness.tsx` | Rewritten: 3 cards (pipeline / agents-as-code / MCP) + new mapping table + imports report JSON |
| `site/tsconfig.json` | `resolveJsonModule: true` added so the JSON import typechecks |
| `.github/workflows/deploy-pages.yml` | `paths` now includes `data/generated/contextualization_report.json` so Pages rebuilds when report metrics change |

Site JSON import path: `import report from "../../../data/generated/contextualization_report.json"` from `site/src/sections/CdfReadiness.tsx`. Site `vite build` **succeeded** after this change.

**Not done:** browser walkthrough of `/cdf` in the app and `#cdf` on the site. Do that as T1-G.

---

## What is NOT done (Tier 1 remaining)

These are specified in `TIER1.md`. Summary:

| ID | Task | Why it is still open |
|---|---|---|
| T1-A | Toolkit YAML: spaces, `PharmaDeviation` container+view+datamodel, Records stream | `cdf/modules/pharmaops_deviation/` only has `agents/` |
| T1-B | Rewrite `COGNITE_MAPPING.md` | Still the old CogniteAsset / CogniteActivity table |
| T1-C | Rewrite README MCP / agents-as-code / contextualization / structure | Still describes a single Python generator writing clean JSON |
| T1-D | EVALS.md Atlas export note; fix stale "12-case" copy | `PROJECT_BRIEF.md`, `AboutPage.tsx`, `TechStack.tsx` still say 12 |
| T1-E | Update stub comments + About + site Hero/GettingStarted/HowItWorks/TechStack/Problem | Mapping comments in `CdfDataProvider.stub.ts` still say CogniteActivity for events |
| T1-F | CI: `export:atlas` drift check; Python not required unless you add generate-data | `ci.yml` has no atlas drift step |
| T1-G | Typecheck + lint + test + eval + site build + **browser verify** | typecheck aborted; UI unverified |
| T1-H | Commit + push + confirm CI and Pages | **Only if the user asks** |

`docs/handoff/` (this folder) did not exist at freeze; creating it is the current session's deliverable.

---

## Uncommitted file inventory at freeze

Modified:

```
.github/workflows/deploy-pages.yml
app/src/components/CdfReadinessPanel.tsx
app/src/pages/DataModelPage.tsx
app/src/types/cdfMapping.ts
data/generated/{events,relationships,timeSeries,workOrders}.json
public/data/generated/{same}
package.json
package-lock.json
scripts/generate_synthetic_pharma_data.py
site/src/sections/CdfReadiness.tsx
site/tsconfig.json
```

Untracked:

```
app/src/components/ContextualizationPanel.tsx
app/src/types/contextualization.ts
cdf/                          # entire tree
data/generated/contextualization_report.json
data/raw/                     # entire tree
mcp.json.example
public/data/generated/contextualization_report.json
scripts/contextualize.py
scripts/export-atlas-agent.ts
server/mcp/                   # entire tree
```

Plus, after this handoff session: `docs/handoff/**`.

`package-lock.json` changed because `@modelcontextprotocol/sdk` and `js-yaml` were added. Keep both.

---

## Commands that must stay green

From repo root (Node 20+, Python 3, no extra pip packages):

```bash
npm ci
npm run generate-data          # python3 required
npm run check-data-sync
npm run lint
npm run typecheck
npm test                       # includes server/mcp/server.test.ts
npm run eval                   # 18/18
npm run export:atlas           # then: git diff --exit-code cdf/  (after first commit of generated files)
npm run build
cd site && npm ci && npm run build
```

Dev:

```bash
npm run dev                    # Vite :5173 + Express :3001
npm run mcp                    # stdio MCP; do not run as a background leftover
cd site && npm run dev         # docs site (base /PharmaOpsCopilot/ in production)
```

Sandbox note: `tsx` / `vitest` sometimes hit `EPERM` in the Cursor sandbox. Re-run with full permissions (`required_permissions: ["all"]`).

---

## Architecture the next agent must not break

```
data/raw/*          siloed IT/OT/ET exports
        │
        ▼
contextualize.py    identity resolution, units, timezones, dedupe, report
        │
        ▼
data/generated/*    unified JSON  ──copy──►  public/data/generated/*
        │                                      │
        ▼                                      ▼
Express + agent tools                    React LocalDataProvider
        │
        ├── POST /api/copilot  (orchestrator + optional OpenAI)
        └── MCP stdio          (same tools)
                │
                ▼
        export:atlas → cdf/ YAML (Atlas / Toolkit / CLI evals)
```

The React app still depends only on `IDataProvider`. `LocalDataProvider` reads `/data/generated`. `CdfDataProvider.stub.ts` stays a stub — **do not implement a live CDF client** in this prototype (no credentials).

---

## Stale copy that will embarrass the sample if left

Search the repo for these and fix as part of T1-D/T1-E:

- `"12-case"` / `"12 case"` / `"12-case eval"`
- Mapping table rows that say Batch/Deviation → **CogniteActivity** as the primary mapping (events go to **Records**; batch goes to **ISA-88 Batch**; deviation is the **extension view**)
- README "Python generator" as if it writes `data/generated` directly
- Site TechStack "12-case eval runner"
- `PROJECT_BRIEF.md` goals still say 12-case and "Python generator" only
- `DEMO_SCRIPT.md` section 4 still walks CogniteAsset/Activity and does not mention contextualization, MCP, or ISA-88/95
- `AboutPage.tsx` "12-case" and "activities/events" as CDM activities
